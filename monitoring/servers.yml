---
# Example config
servers:
  - ip: x.x.x.x
    name: 'Server 1'
    ssh:  # is optional
      ssh_port: 22
      ssh_user: 'user'
      ssh_pubkey_path: '/path/to/key'
    modules:
      - 'sysinfo'

  - ip: localhost
    name: 'LocalServer'
    modules:
      - 'sysinfo'
